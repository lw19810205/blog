# Claude Code 项目配置

## 交互规则

1. **永远用中文回复** —— 所有交流、解释、代码注释均使用中文
2. **改代码后自动检查** —— 每次修改完成后，自动检查是否有明显的语法错误
3. **提交前汇报改动** —— 执行 git commit 前，先列出所有改动的文件及其内容概要
4. **不确定时先 brainstorming** —— 如果需求不明确或方案有多种选择，先用 `brainstorming` 技能理清需求再继续
5. **项目文件目录** —— 项目相关的文档和文件统一放在 `C:\Users\0\docs` 目录下
